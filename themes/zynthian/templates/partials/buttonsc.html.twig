{% set icon = icon|default('') %}
{% set href = href|default('') %}
{% set label = label|default('') %}
{% set class = class|default('btn--primary') %}
{% set href = href|default('') %}
{% set blank = false %}
{% set label = label|default('') %}
{% set icon = icon|default('') %}

{# looks for file in current page medias #}
{% for f in page.media if href == f.filename %}
    {% set href = f.url %}
    {% set blank = true %}
{% endfor %}

<div class="btn {{ class }}">
    <a href="{{ href }}" {% if blank %}target="_blank" rel="noopener noo referrer"{% endif %}>{{ label }}{% if icon %}<span class="material-icons">{{ icon }}</span>{% endif %}</a>
</div>
